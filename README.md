1.REQMENTS:

 	Linux / Windows
 	Python 3.8
 	Libs:
    colorama
    os
    platform
    subprocess
    sys
 
2.SETUP GUIGE

	0.UNPACKAGE
		unpackage ALL zip to something place

		and run "setup.py"

 	1.DO YOU WANT INSTALL HDI
 		answer "Y"

 	2.YOUR USERNAME
		print name of your user directory

	3.Perfect!
		your HDI installed to C:/HDI/HDI.py

			or /home/{username}/HDI/HDI.py

3.RUN

	for run in terminal:
		go to source dir and write "python /HDI.py"

	for run in default compilator:
		open file))

4.USE

    Commands:
     index - Show directory contents
     read - Read a text file
     open - Open a file with default app
     chdir - Change current directory
     cdir - Create dir
     cudir - Show current dir
     exit - Exit the program
     help - Show this guide
     delete file - Delete fil
     delete dir - Delete empty dir
     rename - Rename file or dir
     copy - Copy file
     qr - Show secret QR-code
     credits - Show creators
